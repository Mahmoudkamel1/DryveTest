using DryveTask.ViewModels;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace TaskTest
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {
        }
        [TestClass]
        public class StartingMethod
        {
            [TestMethod]
            [TestCategory("Addommand")]
            public void AddTest()
            {
                // Arrange 
                /*var _mainPageViewModel = new MainPageViewModel();
                _mainPageViewModel.Type = 5;
                _mainPageViewModel.Type1 = 3;

                // Act 
                _mainPageViewModel.AddCommand.Execute(null);

                // Assert
                Assert.IsTrue(_mainPageViewModel.Result == 8, "The Sum is: " + (_mainPageViewModel.Type + _mainPageViewModel.Type1));*/
            }
        }
    }
}
